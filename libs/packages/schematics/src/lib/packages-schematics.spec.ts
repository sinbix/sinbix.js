import { packagesSchematics } from './packages-schematics';

describe('packagesSchematics', () => {
  it('should work', () => {
    expect(packagesSchematics()).toEqual('packages-schematics');
  });
});
